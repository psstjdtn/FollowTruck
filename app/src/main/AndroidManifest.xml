<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shinhan.sspark.followtruck">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SubActivity" />
        <activity android:name=".ComSelActivity" />
        <activity android:name=".CusJoinActivity" />
        <activity android:name=".ComBoardActivity" />
        <activity android:name=".BizButtonActivity" />
        <activity android:name=".BizInfActivity" />
        <activity android:name=".BizMenuActivity" />
        <activity android:name=".BizLocateActivity" />
        <activity android:name=".BizInfChgActivity" />
        <activity android:name=".BizManageActivity" />
        <activity android:name=".ComOrderHisActivity" />
        <activity android:name=".ComNaviActivity" />
        <activity android:name=".ComCenterActivity" />
        <activity android:name=".CusMainActivity" />
        <activity android:name=".CusBizLookupActivity" />
        <activity android:name=".CusOrderActivity" />
        <activity android:name=".ComSignUpActivity"></activity>
    </application>

</manifest>